<div class="w-full">
  <div class="flex justify-start items-center mb-4">
    <h1 class="font-bold text-gray-700 text-2xl mr-3">Клиенты | </h1>

    <%= link_to "Добавить", new_client_path, data: { turbo_frame: 'modal' }, class: "px-4 py-2 transition rounded-xl text-amber-700 hover:text-slate-900 hover:bg-amber-300 border border-amber-300 font-medium text-sm cursor-pointer" %>
  </div>

  <div id="clients" class="grid lg:grid-cols-5 sm:grid-cols-5 gap-3">
    <%= render @clients %>
  </div>
</div>
