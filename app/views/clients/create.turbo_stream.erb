<% if @client.errors.any? %>
  <%= turbo_stream.replace "new-client-form", partial: "clients/form", locals: { client: @client } %> <!-- Заменяем форму на форму с ошибками -->
<% else %>
  <%= turbo_stream.replace "new-client-form", partial: "products/client_select", locals: { form: form } %>
<% end %>
