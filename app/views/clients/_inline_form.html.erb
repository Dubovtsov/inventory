<%= form_with(model: client, class: "contents flex items-center") do |form| %>
  <% if client.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
    <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

    <ul>
      <% client.errors.each do |error| %>
      <li><%= error.full_message %></li>
      <% end %>
    </ul>
    </div>
  <% end %>
  
  <%= form.label "Введите нового поставщика / Enter", class:"font-semibold text-sm text-green-600" %>
  <%= form.text_field :title, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full", placeholder: "Введите нового поставщика" %>

  <%#= form.submit class: "ml-2 px-4 py-2 bg-blue-500 text-white rounded shadow-sm hover:bg-blue-600 focus:outline-none" %>
<% end %>